name: Jekyll

on: [pull_request, push]

jobs:
  build:
    name: Build
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - user: actions/setup-node@v3
          with:
            node-version: 14
        - run: npm i
        - run: jekyll build
